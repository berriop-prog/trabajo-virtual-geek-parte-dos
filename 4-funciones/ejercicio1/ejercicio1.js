'use strict';

/*Crea una función que reciba como argumento dos valores y devuelva como valor de retorno la multiplicación de ambos. */
let num1 = 2;
let num2 = 4;
let total;

function multiplicación(a, b) {
    const total = (a * b);
    return total;
}
console.log(multiplicación(num1, num2))